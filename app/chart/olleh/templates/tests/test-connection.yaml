apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "olleh.fullname" . }}-test-connection"
  labels:
    {{- include "olleh.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "olleh.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
